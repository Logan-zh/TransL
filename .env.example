# DEMOL 桌面版 API 位址（建置時寫入 out/；執行時 .env 或系統環境變數可覆寫）
# 本機開發（後台 docker compose 預設 :3000）：
# TRANSL_API_URL=http://localhost:3000
# 正式（API 子網域）：
TRANSL_API_URL=https://api-demol.towkr.com
# 或與前台同網域（nginx 轉發 /api/）：
# TRANSL_API_URL=https://demol.towkr.com
